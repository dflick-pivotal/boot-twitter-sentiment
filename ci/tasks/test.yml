---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: alpine

inputs:
  - name: boot-twitter-sentiment

run:
  path: mvn
  args: ["-f", "boot-twitter-sentiment/pom.xml", "test"]

params:
  TERM: -dumb
