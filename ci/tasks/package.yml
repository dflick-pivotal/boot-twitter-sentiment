---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: alpine

inputs:
  - name: boot-twitter-sentiment

outputs:
  - name: build

run:
  path: mvn
  args: ["-f", "boot-twitter-sentiment/pom.xml", "package"]

params:
  TERM: -dumb
